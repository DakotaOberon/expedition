/**
* Status constructor
*
* @function		Status(type, [length])
* @param		{enum}		type			StatusType identifier
* @param		{real}		[length]		Length in frames status lasts
*/
function Status(_type, _length=60) constructor {
	type = _type;
	length = _length;
}
